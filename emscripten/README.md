Step:
1. Run build.bat (in Root Folder of repo) to build LNLib.
2. Install Ninja.
3. Install Python and Set Python system environment variables.
4. Build LNLibjs by Emscripten:
    Windows:
        Run build_wasm.ps1 by Windows Powershell.
